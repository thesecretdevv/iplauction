{
  "name": "bidwicket-deploy",
  "private": true,
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "npm ci --prefix auction-backend",
    "start": "npm start --prefix auction-backend"
  }
}
